<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>HGHomeBot+</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=JetBrains+Mono:wght@400;500&family=Noto+Sans+JP:wght@300;400;500&display=swap" rel="stylesheet">

<style>
:root {
  --bg:      #060810;
  --surface: #0b0f1a;
  --border:  #1a2235;
  --border2: #243050;
  --text:    #c9d1e0;
  --muted:   #5a6a85;
  --accent:  #3b82f6;
  --accent2: #60a5fa;
  --glow:    rgba(59,130,246,0.22);
  --glow2:   rgba(99,102,241,0.15);
}

*, *::before, *::after { margin:0; padding:0; box-sizing:border-box; }
html { scroll-behavior:smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Sans JP', sans-serif;
  font-weight: 300;
  overflow-x: hidden;
}

/* ── 背景グリッド + 放射グラデーション強化 ── */
body::before {
  content: "";
  position: fixed;
  inset: 0;
  background-image:
    radial-gradient(ellipse 80% 60% at 20% 10%, rgba(59,130,246,0.09) 0%, transparent 55%),
    radial-gradient(ellipse 60% 50% at 80% 85%, rgba(99,102,241,0.07) 0%, transparent 50%),
    linear-gradient(rgba(59,130,246,0.035) 1px, transparent 1px),
    linear-gradient(90deg, rgba(59,130,246,0.035) 1px, transparent 1px);
  background-size: 100% 100%, 100% 100%, 48px 48px, 48px 48px;
  pointer-events: none;
  z-index: 0;
}

nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  height: 64px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 5%;
  background: rgba(6,8,16,0.88);
  backdrop-filter: blur(14px);
  border-bottom: 1px solid var(--border);
  z-index: 1000;
}

.nav-logo {
  font-family: 'Syne', sans-serif;
  font-size: 18px;
  font-weight: 800;
  letter-spacing: 0.04em;
  color: #fff;
  display: flex;
  align-items: center;
  gap: 8px;
}
.nav-logo span { color: var(--accent); }
.nav-links { display: flex; gap: 8px; }
.nav-links a {
  color: var(--muted);
  text-decoration: none;
  font-family: 'JetBrains Mono', monospace;
  font-size: 12px;
  padding: 6px 14px;
  border-radius: 6px;
  border: 1px solid transparent;
  transition: all 0.25s;
}
.nav-links a:hover {
  color: var(--accent2);
  border-color: var(--border2);
  background: rgba(59,130,246,0.08);
}

.hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  cursor: pointer;
  padding: 6px;
}
.hamburger span {
  display: block;
  width: 22px; height: 2px;
  background: var(--text);
  border-radius: 2px;
  transition: 0.3s;
}
.hamburger.open span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
.hamburger.open span:nth-child(2) { opacity: 0; }
.hamburger.open span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }

.mobile-menu {
  display: none;
  position: fixed;
  top: 64px; left: 0; right: 0;
  background: rgba(6,8,16,0.97);
  border-bottom: 1px solid var(--border);
  backdrop-filter: blur(14px);
  z-index: 999;
  flex-direction: column;
  padding: 16px 5%;
  gap: 4px;
}
.mobile-menu.open { display: flex; }
.mobile-menu a {
  color: var(--text);
  text-decoration: none;
  font-family: 'JetBrains Mono', monospace;
  font-size: 14px;
  padding: 12px 16px;
  border-radius: 8px;
  border: 1px solid transparent;
  transition: 0.2s;
}
.mobile-menu a:hover {
  background: rgba(59,130,246,0.08);
  border-color: var(--border2);
  color: var(--accent2);
}

section {
  position: relative;
  z-index: 1;
  min-height: 100vh;
  padding: 120px 8% 80px;
  scroll-margin-top: 64px;
}
.section-header { margin-bottom: 48px; }
.section-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: 11px;
  color: var(--accent);
  letter-spacing: 0.18em;
  text-transform: uppercase;
  margin-bottom: 12px;
}
h2 {
  font-family: 'Syne', sans-serif;
  font-size: clamp(28px, 5vw, 44px);
  font-weight: 700;
  color: #fff;
  line-height: 1.15;
}
.section-desc { margin-top: 10px; color: var(--muted); font-size: 14px; }

.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 16px;
  transition: border-color 0.3s, box-shadow 0.3s, transform 0.3s;
  position: relative;
  overflow: hidden;
}
.card::before {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(59,130,246,0.06), transparent 60%);
  opacity: 0;
  transition: opacity 0.3s;
}
.card:hover {
  border-color: var(--border2);
  box-shadow: 0 0 0 1px var(--border2), 0 8px 40px rgba(0,0,0,0.6);
  transform: translateY(-3px);
}
.card:hover::before { opacity: 1; }
.card-title {
  font-family: 'Syne', sans-serif;
  font-size: 16px;
  font-weight: 600;
  color: #fff;
  margin-bottom: 8px;
}
.card p { color: var(--muted); font-size: 14px; line-height: 1.7; }

.btn {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  margin-top: 16px;
  padding: 10px 22px;
  border-radius: 8px;
  text-decoration: none;
  background: linear-gradient(135deg, var(--accent), #6366f1);
  color: white;
  font-family: 'JetBrains Mono', monospace;
  font-size: 13px;
  font-weight: 500;
  letter-spacing: 0.03em;
  transition: all 0.25s;
  border: 1px solid transparent;
  box-shadow: 0 4px 20px rgba(59,130,246,0.3);
}
.btn:hover {
  background: transparent;
  border-color: var(--accent);
  color: var(--accent2);
  box-shadow: 0 0 28px var(--glow);
}

#home {
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding-top: 140px;
}

.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(59,130,246,0.12);
  border: 1px solid rgba(59,130,246,0.3);
  border-radius: 100px;
  padding: 5px 14px;
  font-family: 'JetBrains Mono', monospace;
  font-size: 11px;
  color: var(--accent2);
  margin-bottom: 24px;
  width: fit-content;
  box-shadow: 0 0 20px rgba(59,130,246,0.1);
}
.hero-badge::before {
  content: "";
  width: 6px; height: 6px;
  border-radius: 50%;
  background: var(--accent);
  animation: pulse 2s infinite;
}
@keyframes pulse {
  0%, 100% { opacity: 1; box-shadow: 0 0 0 0 rgba(59,130,246,0.5); }
  50% { opacity: 0.4; box-shadow: 0 0 0 5px rgba(59,130,246,0); }
}

.hero-title {
  font-family: 'Syne', sans-serif;
  font-size: clamp(36px, 7vw, 72px);
  font-weight: 800;
  line-height: 1.05;
  color: #fff;
  margin-bottom: 20px;
}
.hero-title .accent {
  background: linear-gradient(135deg, var(--accent), #818cf8);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
.hero-sub {
  color: var(--muted);
  font-size: clamp(14px, 2vw, 16px);
  max-width: 500px;
  line-height: 1.8;
  margin-bottom: 40px;
}

.update-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  overflow: hidden;
  margin-bottom: 16px;
  transition: border-color 0.3s, transform 0.3s;
}
.update-card:hover { border-color: var(--border2); transform: translateY(-2px); }

.update-card.latest {
  border-color: rgba(59,130,246,0.4);
  box-shadow: 0 0 0 1px rgba(59,130,246,0.14), 0 8px 40px rgba(0,0,0,0.5);
}
.update-card.latest .update-header {
  background: linear-gradient(135deg, rgba(59,130,246,0.1), rgba(99,102,241,0.05) 50%, transparent 80%);
}

.update-header {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 18px 24px;
  border-bottom: 1px solid var(--border);
  cursor: pointer;
  user-select: none;
}
.version-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: 12px;
  font-weight: 500;
  color: var(--accent2);
  background: rgba(59,130,246,0.1);
  border: 1px solid rgba(59,130,246,0.2);
  padding: 3px 10px;
  border-radius: 100px;
}
.version-tag.new {
  color: #fff;
  background: linear-gradient(135deg, var(--accent), #6366f1);
  border-color: transparent;
  box-shadow: 0 2px 12px rgba(59,130,246,0.4);
}
.update-header h3 {
  font-family: 'Syne', sans-serif;
  font-size: 15px;
  font-weight: 600;
  color: #fff;
  flex: 1;
}
.chevron { color: var(--muted); font-size: 12px; transition: transform 0.3s; }
.update-card.open .chevron { transform: rotate(180deg); }
.update-body { display: none; padding: 20px 24px; }
.update-card.open .update-body { display: block; }

.update-images {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  gap: 10px;
  margin-bottom: 16px;
}
.update-images img {
  width: 100%;
  aspect-ratio: 16/9;
  object-fit: cover;
  border-radius: 8px;
  border: 1px solid var(--border);
  cursor: pointer;
  transition: border-color 0.2s, transform 0.2s;
}
.update-images img:hover { border-color: var(--accent); transform: scale(1.02); }

.update-list { list-style: none; }
.update-list li {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  padding: 7px 0;
  color: var(--text);
  font-size: 14px;
  line-height: 1.6;
  border-bottom: 1px solid rgba(255,255,255,0.03);
}
.update-list li:last-child { border-bottom: none; }
.update-list li::before {
  content: "→";
  color: var(--accent);
  font-family: 'JetBrains Mono', monospace;
  font-size: 12px;
  margin-top: 2px;
  flex-shrink: 0;
}

.update-img-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: 11px;
  color: var(--accent);
  letter-spacing: 0.12em;
  text-transform: uppercase;
  margin-bottom: 10px;
  margin-top: 4px;
}
.update-images--single { grid-template-columns: minmax(0, 480px); }
.update-images--compare { grid-template-columns: repeat(2, 1fr); }
@media (max-width: 600px) { .update-images--compare { grid-template-columns: 1fr; } }

.compare-wrap { position: relative; }
.compare-label {
  position: absolute;
  top: 8px; left: 8px;
  font-family: 'JetBrains Mono', monospace;
  font-size: 10px;
  color: #fff;
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(255,255,255,0.15);
  padding: 2px 8px;
  border-radius: 4px;
  letter-spacing: 0.08em;
  pointer-events: none;
}
.compare-wrap img {
  width: 100%;
  aspect-ratio: 16/9;
  object-fit: cover;
  border-radius: 8px;
  border: 1px solid var(--border);
  cursor: pointer;
  transition: border-color 0.2s, transform 0.2s;
  display: block;
}
.compare-wrap img:hover { border-color: var(--accent); transform: scale(1.02); }

.roadmap-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 14px;
}
.roadmap-item {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 20px;
  display: flex;
  align-items: flex-start;
  gap: 14px;
  transition: border-color 0.3s, transform 0.3s, box-shadow 0.3s;
  position: relative;
  overflow: hidden;
}
.roadmap-item::before {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(59,130,246,0.05), transparent 60%);
  opacity: 0;
  transition: opacity 0.3s;
}
.roadmap-item:hover { border-color: var(--border2); transform: translateY(-2px); box-shadow: 0 8px 30px rgba(0,0,0,0.4); }
.roadmap-item:hover::before { opacity: 1; }
.roadmap-item.done { opacity: 0.45; border-style: dashed; }
.roadmap-item.done .roadmap-icon::after { content: " ✓"; font-size: 14px; color: #57F287; }
.roadmap-icon { font-size: 20px; flex-shrink: 0; margin-top: 2px; }
.roadmap-item p { font-size: 14px; color: var(--text); line-height: 1.6; }
.roadmap-item.done p { text-decoration: line-through; color: var(--muted); }

.command-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
  gap: 12px;
}
.command-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 18px 20px;
  transition: border-color 0.3s, transform 0.3s, box-shadow 0.3s;
  position: relative;
  overflow: hidden;
}
.command-card::before {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(59,130,246,0.07), transparent 60%);
  opacity: 0;
  transition: opacity 0.3s;
}
.command-card:hover {
  border-color: var(--accent);
  transform: translateY(-2px);
  box-shadow: 0 0 0 1px rgba(59,130,246,0.2), 0 8px 30px rgba(59,130,246,0.12);
}
.command-card:hover::before { opacity: 1; }
.command-name {
  font-family: 'JetBrains Mono', monospace;
  font-size: 13px;
  font-weight: 500;
  color: var(--accent2);
  margin-bottom: 6px;
  position: relative;
}
.command-desc { font-size: 13px; color: var(--muted); line-height: 1.5; position: relative; }

.other-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  gap: 14px;
  margin-top: 12px;
}
.other-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 20px;
  text-decoration: none;
  display: block;
  transition: border-color 0.3s, transform 0.3s;
}
.other-card:hover { border-color: var(--border2); transform: translateY(-2px); }
.other-card .card-title { margin-bottom: 4px; }

.discord-wrap {
  border-radius: 10px;
  overflow: hidden;
  border: 1px solid var(--border);
  background: #111;
  margin-bottom: 14px;
}
.discord-wrap iframe { display: block; }

.update-divider { border: none; border-top: 1px solid var(--border); margin: 20px 0; }

.lightbox {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.92);
  z-index: 9999;
  align-items: center;
  justify-content: center;
  padding: 20px;
}
.lightbox.active { display: flex; }
.lightbox img {
  max-width: 90vw;
  max-height: 85vh;
  border-radius: 10px;
  object-fit: contain;
  border: 1px solid var(--border2);
}
.lightbox-close {
  position: fixed;
  top: 20px; right: 24px;
  color: var(--muted);
  font-size: 28px;
  cursor: pointer;
  line-height: 1;
  transition: color 0.2s;
}
.lightbox-close:hover { color: #fff; }

footer {
  position: relative;
  z-index: 1;
  text-align: center;
  padding: 36px 20px;
  border-top: 1px solid var(--border);
  color: var(--muted);
  font-family: 'JetBrains Mono', monospace;
  font-size: 12px;
  background: linear-gradient(to bottom, transparent, rgba(59,130,246,0.03));
}

.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}
.fade-in.visible { opacity: 1; transform: translateY(0); }

@media (max-width: 768px) {
  section { padding: 100px 5% 60px; }
  .nav-links { display: none; }
  .hamburger { display: flex; }
  h2 { font-size: 28px; }
  .hero-title { font-size: 38px; }
  .update-images { grid-template-columns: 1fr; }
  .command-grid { grid-template-columns: repeat(auto-fill, minmax(160px, 1fr)); }
  .other-grid { grid-template-columns: 1fr; }
}
@media (max-width: 480px) {
  .hero-title { font-size: 30px; }
  .roadmap-grid { grid-template-columns: 1fr; }
}
</style>
</head>

<body>

<nav>
  <div class="nav-logo">HGHome<span>Bot+</span></div>
  <div class="nav-links">
    <a href="#home">ホーム</a>
    <a href="#roadmap">ロードマップ</a>
    <a href="#commands">コマンド</a>
    <a href="#other">その他</a>
  </div>
  <div class="hamburger" id="hamburger">
    <span></span><span></span><span></span>
  </div>
</nav>

<div class="mobile-menu" id="mobileMenu">
  <a href="#home" onclick="closeMobile()">ホーム</a>
  <a href="#roadmap" onclick="closeMobile()">ロードマップ</a>
  <a href="#commands" onclick="closeMobile()">コマンド</a>
  <a href="#other" onclick="closeMobile()">その他</a>
</div>

<section id="home">
  <div class="fade-in">
    <div class="hero-badge">v0.5.3 リリース済み</div>
    <h1 class="hero-title">ちょっと多機能な<br><span class="accent">管理 & カジノ</span><br>Discord Bot</h1>
    <p class="hero-sub">深夜テンションで作った…<br>ProBotのXP有料化をきっかけにXP機能が追加されました。</p>
    <a href="#" class="btn">サーバーに追加できると思ったかぁあ？</a>
  </div>

  <div style="margin-top:64px;">
    <div class="section-header fade-in">
      <div class="section-label">// changelog</div>
      <h2>アップデート履歴</h2>
    </div>

    <!-- v0.5.3 (LATEST) -->
    <div class="update-card latest open fade-in">
      <div class="update-header" onclick="toggleUpdate(this)">
        <span class="version-tag new">v0.5.3</span>
        <h3>チャンネル設定コマンドを /set-channel に統合</h3>
        <span class="chevron">▼</span>
      </div>
      <div class="update-body">
        <p class="update-img-label">コマンド整理・統合</p>
        <ul class="update-list">
          <li>チャンネル設定系コマンドを <code style="color:var(--accent2);font-family:'JetBrains Mono',monospace;">/set-channel type:</code> に一本化</li>
          <li><code style="color:var(--accent2);font-family:'JetBrains Mono',monospace;">/set_count</code>・<code style="color:var(--accent2);font-family:'JetBrains Mono',monospace;">/set_welcome</code>・<code style="color:var(--accent2);font-family:'JetBrains Mono',monospace;">/set-x</code>・<code style="color:var(--accent2);font-family:'JetBrains Mono',monospace;">/set-server</code>・<code style="color:var(--accent2);font-family:'JetBrains Mono',monospace;">/set_xp_channel</code>・<code style="color:var(--accent2);font-family:'JetBrains Mono',monospace;">/set-xpnews</code> を廃止</li>
          <li>type は <strong>count / welcome / x / server / levelup / xpnews</strong> の 6 種類から選択</li>
          <li>各 Cog（count・welcome・x_notifier・server・xp）から設定ロジックを分離・setchannel.py に集約</li>
          <li>コマンド数を削減しつつ機能はそのまま維持</li>
        </ul>
        <div class="update-divider"></div>
        <p class="update-img-label">使い方</p>
        <ul class="update-list">
          <li>/set-channel type:count channel:#ch → カウントチャンネル設定 & カウントリセット</li>
          <li>/set-channel type:welcome channel:#ch → 入退室通知チャンネル設定</li>
          <li>/set-channel type:x channel:#ch → X 通知チャンネル設定</li>
          <li>/set-channel type:server channel:#ch → 統計レポートチャンネル設定</li>
          <li>/set-channel type:levelup channel:#ch → レベルアップ通知チャンネル設定</li>
          <li>/set-channel type:xpnews channel:#ch → XP ニュースチャンネル設定</li>
        </ul>
      </div>
    </div>

    <!-- v0.5.1 -->
    <div class="update-card fade-in">
      <div class="update-header" onclick="toggleUpdate(this)">
        <span class="version-tag">v0.5.1</span>
        <h3>サーバー統計 強化・週間 / 月間レポート追加</h3>
        <span class="chevron">▼</span>
      </div>
      <div class="update-body">
        <p class="update-img-label">サーバー統計アップデート</p>
        <ul class="update-list">
          <li>週間・月間サーバーレポートを自動送信</li>
          <li>今週 / 今月で一番使われた絵文字ランキングを追加</li>
          <li>1日あたりの平均メッセージ数を算出・表示</li>
          <li>今週・今月で最もアクティブだった日を表示</li>
          <li>リアクション統計を Top3 / Top5 形式で表示</li>
          <li>既存のデイリーレポートとの互換性を維持</li>
        </ul>
        <div class="update-divider"></div>
        <p class="update-img-label">内部改善</p>
        <ul class="update-list">
          <li>日次データを基にした集計ロジックを共通化</li>
          <li>週間・月間処理を同一コードで管理</li>
          <li>FireBase 読み取り回数を最小化</li>
        </ul>
      </div>
    </div>

    <!-- v0.5 -->
    <div class="update-card fade-in">
      <div class="update-header" onclick="toggleUpdate(this)">
        <span class="version-tag">v0.5</span>
        <h3>カジノ大幅強化・Firebase最適化・/help刷新</h3>
        <span class="chevron">▼</span>
      </div>
      <div class="update-body">

        <p class="update-img-label">カジノ コマンドデザイン リニューアル</p>
        <div class="update-images update-images--compare" style="margin-bottom:16px;">
          <div class="compare-wrap">
            <span class="compare-label">Before</span>
            <img src="image/casino-old.jpeg" alt="旧カジノデザイン" onclick="openLightbox(this.src)">
          </div>
          <div class="compare-wrap">
            <span class="compare-label">After</span>
            <img src="image/casino-new.jpeg" alt="新カジノデザイン" onclick="openLightbox(this.src)">
          </div>
        </div>
        <ul class="update-list" style="margin-bottom:20px;">
          <li>全カジノコマンドの出力を discord.Embed に刷新</li>
          <li>勝利・敗北・引き分けで色が変化（緑 / 赤 / グレー）</li>
          <li>/balance コマンドを新たに追加（アバター付き残高表示）</li>
          <li>ダイスの出目表示を絵文字（⚀〜⚅）に変更</li>
          <li>最大ベット額（10,000,000コイン）の上限を設定</li>
        </ul>

        <div class="update-divider"></div>

        <p class="update-img-label">ブラックジャック 追加</p>
        <div class="update-images update-images--single" style="margin-bottom:16px;">
          <img src="image/blackjack.jpeg" alt="ブラックジャック" onclick="openLightbox(this.src)">
        </div>
        <ul class="update-list" style="margin-bottom:20px;">
          <li>/blackjack コマンドを新規追加</li>
          <li>Hit（カードを引く）/ Stand（止める）/ Double（倍掛け）の3択ボタン操作</li>
          <li>ナチュラルブラックジャックは1.5倍払い（ベット×2.5）</li>
          <li>ディーラーは17以上になるまで自動ドロー</li>
          <li>引き分け時はベット返却、60秒無操作でボタン自動無効化</li>
          <li>他ユーザーによるボタン操作を弾く処理を実装</li>
        </ul>

        <div class="update-divider"></div>

        <p class="update-img-label">Firebase 無料枠 対策</p>
        <ul class="update-list" style="margin-bottom:20px;">
          <li>メッセージカウントの読み書きを Firestore Transaction に移行し競合状態を解消</li>
          <li>Firestore 呼び出しを asyncio.to_thread でラップし非同期処理を正規化</li>
          <li>不要な get → set の二重アクセスを削減、読み取り数を削減</li>
          <li>カジノコマンドのコイン操作を共通ヘルパー（validate_bet）に集約しアクセス回数を最小化</li>
        </ul>

        <div class="update-divider"></div>

        <p class="update-img-label">/help コマンド 刷新</p>
        <ul class="update-list">
          <li>/help の表示内容を現在の全コマンドに合わせて最新化</li>
          <li>カテゴリ別（管理 / カジノ / レベル / X通知 / その他）に整理し見やすく刷新</li>
          <li>各コマンドに簡易説明と使用例を追記</li>
          <li>v0.4〜v0.5 で追加・変更されたコマンドをすべて反映</li>
        </ul>

        <p class="update-img-label">/ping追加</p>
        <ul class="update-list">
          <li>/ping 応答速度を調べます</li>
        </ul>
      </div>
    </div>

    <!-- v0.4.2 -->
    <div class="update-card fade-in">
      <div class="update-header" onclick="toggleUpdate(this)">
        <span class="version-tag">v0.4.2</span>
        <h3>ロールコマンド刷新・remove追加</h3>
        <span class="chevron">▼</span>
      </div>
      <div class="update-body">
        <p class="update-img-label">ロールコマンド</p>
        <ul class="update-list">
          <li>/giverole を /role type: に統合・刷新</li>
          <li>type は give（付与）/ remove（削除）から選択（Discord上でも選択肢表示）</li>
          <li>ロールの付与・削除をひとつのコマンドで管理できるように</li>
        </ul>
      </div>
    </div>

    <!-- v0.4.1 -->
    <div class="update-card fade-in">
      <div class="update-header" onclick="toggleUpdate(this)">
        <span class="version-tag">v0.4.1</span>
        <h3>X（Twitter）通知機能追加</h3>
        <span class="chevron">▼</span>
      </div>
      <div class="update-body">
        <p class="update-img-label">X 通知</p>
        <ul class="update-list">
          <li>Nitter RSS を利用した X（Twitter）新着ポスト通知を実装</li>
          <li>ログイン・APIキー不要で動作</li>
          <li>/set-x channel: type: で通知チャンネルと通知形式を設定</li>
          <li>type は embed（内容も表示）/ link（URLのみ）から選択可能</li>
          <li>/set-xaccount username: で監視アカウントを追加・削除（トグル式）</li>
          <li>/x-list で監視中アカウントと現在の設定を一覧表示</li>
          <li>5分ごとに自動チェック、複数インスタンスへのフォールバック対応</li>
          <li>RT・リプライ・新規ポストを色分けで識別</li>
          <li>設定は Firebase（guilds/id/x_channel・x_notify_type・x/）に保存</li>
        </ul>
      </div>
    </div>

    <!-- v0.4 -->
    <div class="update-card fade-in">
      <div class="update-header" onclick="toggleUpdate(this)">
        <span class="version-tag">v0.4</span>
        <h3>管理強化・ロールボタン・サーバー統計</h3>
        <span class="chevron">▼</span>
      </div>
      <div class="update-body">
        <p class="update-img-label">管理コマンド</p>
        <ul class="update-list" style="margin-bottom:20px;">
          <li>/kick を追加。理由を任意で指定可能、自分自身へのキックは不可</li>
          <li>/ban を追加。理由・メッセージ削除日数（0〜7日）を任意指定可能、省略時は削除なし</li>
        </ul>

        <div class="update-divider"></div>

        <p class="update-img-label">ロール付与ボタン</p>
        <ul class="update-list" style="margin-bottom:20px;">
          <li>/role-button role: message: でボタン付きメッセージを任意チャンネルに投稿</li>
          <li>ボタンを押すとロールを付与、もう一度押すと削除（トグル式）</li>
          <li>設定内容（チャンネル・メッセージ・ロール）を Firebase に保存</li>
          <li>Bot 再起動後もボタンが正常動作するよう永続化対応</li>
        </ul>

        <div class="update-divider"></div>

        <p class="update-img-label">サーバー統計</p>
        <ul class="update-list" style="margin-bottom:20px;">
          <li>/set-server で統計レポートの送信先チャンネルを設定</li>
          <li>毎日 JST 0時に前日のメッセージ数・メンバー増減・最多リアクション絵文字を自動送信</li>
          <li>過去30日間のメッセージ数・参加・退出を線グラフ画像で添付</li>
          <li>日次データを Firebase（guilds/id/server/日付）に随時保存</li>
        </ul>

        <div class="update-divider"></div>

        <p class="update-img-label">ボーナスコマンド統合</p>
        <ul class="update-list">
          <li>/daily・/weekly・/monthly を /bonus type: に統合</li>
          <li>type は daily / weekly / monthly から選択（Discord上でも選択肢表示）</li>
          <li>クールダウン残り時間の表示を日・時間・分・秒で統一</li>
        </ul>
      </div>
    </div>

    <!-- v0.3.1 -->
    <div class="update-card fade-in">
      <div class="update-header" onclick="toggleUpdate(this)">
        <span class="version-tag">v0.3.1</span>
        <h3>カウント制限・/top リニューアル</h3>
        <span class="chevron">▼</span>
      </div>
      <div class="update-body">
        <p class="update-img-label">カウント連続投稿制限</p>
        <div class="update-images update-images--single">
          <img src="image/count.jpeg" alt="カウント制限" onclick="openLightbox(this.src)">
        </div>
        <ul class="update-list" style="margin-bottom:20px;">
          <li>カウントチャンネルでの連続投稿を3回までに制限</li>
          <li>4回連続で投稿するとカウントが1にリセット</li>
        </ul>
        <p class="update-img-label">/top コマンド リニューアル</p>
        <div class="update-images update-images--compare">
          <div class="compare-wrap">
            <span class="compare-label">Before</span>
            <img src="image/top-old.jpeg" alt="旧 /top" onclick="openLightbox(this.src)">
          </div>
          <div class="compare-wrap">
            <span class="compare-label">After</span>
            <img src="image/top-new.jpeg" alt="新 /top" onclick="openLightbox(this.src)">
          </div>
        </div>
        <ul class="update-list">
          <li>/top コマンドのUIをリニューアル</li>
          <li>◀ ▶ ボタンでランキング種別を切り替え可能に</li>
          <li>20秒無操作でボタンが自動無効化</li>
          <li>逆所持金ランキング（ワースト）を追加</li>
          <li>合計資産ランキングにドル換算が反映されていないバグを修正</li>
        </ul>
        <div class="update-divider"></div>
        <p class="update-img-label">コイン関連アップデート</p>
        <ul class="update-list">
          <li>/steal の罰金を「盗む予定だった金額の1/10」に変更</li>
          <li>罰金が払えない場合は借金状態になるように</li>
          <li>/profile で借金時に赤表示・警告を追加</li>
        </ul>
      </div>
    </div>

    <!-- v0.3 -->
    <div class="update-card fade-in">
      <div class="update-header" onclick="toggleUpdate(this)">
        <span class="version-tag">v0.3</span>
        <h3>ようこそ・カウント機能追加</h3>
        <span class="chevron">▼</span>
      </div>
      <div class="update-body">
        <ul class="update-list">
          <li>ようこそ・さようならのメッセージを追加</li>
          <li>カウント機能の追加</li>
          <li>銀行のコマンドにallを追加</li>
          <li>銀行の手数料を削除</li>
        </ul>
      </div>
    </div>

    <!-- v0.2 -->
    <div class="update-card fade-in">
      <div class="update-header" onclick="toggleUpdate(this)">
        <span class="version-tag">v0.2</span>
        <h3>XP・カジノ機能実装</h3>
        <span class="chevron">▼</span>
      </div>
      <div class="update-body">
        <ul class="update-list">
          <li>Botが起動しない問題を修正</li>
          <li>XPレベリング機能を実装</li>
          <li>コイン・カジノ機能を追加</li>
          <li>Monthlyボーナスを追加</li>
          <li>窃盗成功率を上方修正</li>
        </ul>
      </div>
    </div>

    <!-- v0.1 -->
    <div class="update-card fade-in">
      <div class="update-header" onclick="toggleUpdate(this)">
        <span class="version-tag">v0.1</span>
        <h3>初期リリース</h3>
        <span class="chevron">▼</span>
      </div>
      <div class="update-body">
        <ul class="update-list">
          <li>Youtube検索機能</li>
          <li>ロール付与・タイムアウト</li>
          <li>チケット・ヘルプ機能</li>
        </ul>
      </div>
    </div>

  </div>
</section>

<!-- ロードマップ -->
<section id="roadmap">
  <div class="section-header fade-in">
    <div class="section-label">// roadmap</div>
    <h2>開発予定</h2>
    <p class="section-desc">今後追加・改善予定の機能</p>
  </div>
  <div class="roadmap-grid">
    <div class="roadmap-item fade-in"><span class="roadmap-icon">🔢</span><p>カウントの継続率表示</p></div>
    <div class="roadmap-item done fade-in"><span class="roadmap-icon">🔨</span><p>キック・バン機能の実装</p></div>
    <div class="roadmap-item done fade-in"><span class="roadmap-icon">🃏</span><p>ブラックジャック実装</p></div>
    <div class="roadmap-item fade-in"><span class="roadmap-icon">🌏</span><p>グロチャ機能の実装</p></div>
    <div class="roadmap-item fade-in"><span class="roadmap-icon">📊</span><p>Webダッシュボード実装</p></div>
    <div class="roadmap-item fade-in"><span class="roadmap-icon">👤</span><p>安価機能追加</p></div>
    <div class="roadmap-item fade-in"><span class="roadmap-icon">✨</span><p>UI / UX 改善</p></div>
    <div class="roadmap-item fade-in"><span class="roadmap-icon">▶️</span><p>Youtube検索復活</p></div>
  </div>
</section>

<!-- コマンド一覧 -->
<section id="commands">
  <div class="section-header fade-in">
    <div class="section-label">// commands</div>
    <h2>コマンド一覧</h2>
    <p class="section-desc">管理・カジノ・レベリング</p>
  </div>
  <div class="command-grid">
    <div class="command-card fade-in"><div class="command-name">/help</div><div class="command-desc">ヘルプを表示（カテゴリ別）</div></div>
    <div class="command-card fade-in"><div class="command-name">/timeout</div><div class="command-desc">ユーザーをタイムアウト</div></div>
    <div class="command-card fade-in"><div class="command-name">/kick</div><div class="command-desc">ユーザーをキック</div></div>
    <div class="command-card fade-in"><div class="command-name">/ban</div><div class="command-desc">ユーザーをBAN</div></div>
    <div class="command-card fade-in"><div class="command-name">/role type:</div><div class="command-desc">ロールの付与・削除（give / remove）</div></div>
    <div class="command-card fade-in"><div class="command-name">/role-button</div><div class="command-desc">ロール付与ボタン付きメッセージを投稿</div></div>
    <div class="command-card fade-in"><div class="command-name">/set-channel type: <span style="color:var(--accent);font-size:11px;">NEW</span></div><div class="command-desc">各種チャンネルを一括設定（count / welcome / x / server / levelup / xpnews）</div></div>
    <div class="command-card fade-in"><div class="command-name">/set-xaccount</div><div class="command-desc">監視するXアカウントを追加・削除</div></div>
    <div class="command-card fade-in"><div class="command-name">/set-xnotifytype</div><div class="command-desc">X通知の表示タイプを変更（embed / link）</div></div>
    <div class="command-card fade-in"><div class="command-name">/x-list</div><div class="command-desc">監視中のXアカウント一覧を表示</div></div>
    <div class="command-card fade-in"><div class="command-name">@HGHomeBot+</div><div class="command-desc">AIに質問</div></div>
    <div class="command-card fade-in"><div class="command-name">/work</div><div class="command-desc">働いてコイン獲得</div></div>
    <div class="command-card fade-in"><div class="command-name">/steal</div><div class="command-desc">他ユーザーから盗む</div></div>
    <div class="command-card fade-in"><div class="command-name">/balance</div><div class="command-desc">所持コイン残高を確認</div></div>
    <div class="command-card fade-in"><div class="command-name">/profile</div><div class="command-desc">プロフィール・所持コイン確認</div></div>
    <div class="command-card fade-in"><div class="command-name">/top</div><div class="command-desc">資産ランキング</div></div>
    <div class="command-card fade-in"><div class="command-name">/rank</div><div class="command-desc">レベル表示</div></div>
    <div class="command-card fade-in"><div class="command-name">/leaderboard</div><div class="command-desc">XPランキング</div></div>
    <div class="command-card fade-in"><div class="command-name">/bonus type:</div><div class="command-desc">daily / weekly / monthly ボーナスを受け取る</div></div>
    <div class="command-card fade-in"><div class="command-name">/cointoss</div><div class="command-desc">コイントスで勝負</div></div>
    <div class="command-card fade-in"><div class="command-name">/slot</div><div class="command-desc">スロットで遊ぶ</div></div>
    <div class="command-card fade-in"><div class="command-name">/dice</div><div class="command-desc">数字を予想して賭ける</div></div>
    <div class="command-card fade-in"><div class="command-name">/blackjack</div><div class="command-desc">ブラックジャック</div></div>
  </div>
</section>

<!-- その他 -->
<section id="other">
  <div class="section-header fade-in">
    <div class="section-label">// other</div>
    <h2>その他</h2>
    <p class="section-desc">連絡先・外部リンク</p>
  </div>

  <div class="discord-wrap fade-in">
    <iframe
      src="https://discord.com/widget?id=1434416718897479720&theme=dark"
      width="100%" height="400"
      allowtransparency="true" frameborder="0"
      sandbox="allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts">
    </iframe>
  </div>

  <div class="other-grid">
    <div class="other-card fade-in">
      <div class="card-title">👤 製作者</div>
      <p class="command-desc">Sou930（Discord DM）</p>
    </div>
    <div class="other-card fade-in">
      <div class="card-title">🐙 GitHub</div>
      <p class="command-desc">そのうちちゃんと整えます</p>
    </div>
    <a href="https://hghomebot-plus.onrender.com/web/sns/sns.html" class="other-card fade-in">
      <div class="card-title">🧪 HGSNS</div>
      <p class="command-desc">試作品</p>
    </a>
  </div>
</section>

<footer>© 2026 HGHome Official</footer>

<div class="lightbox" id="lightbox" onclick="closeLightbox()">
  <div class="lightbox-close" onclick="closeLightbox()">✕</div>
  <img id="lightboxImg" src="" alt="">
</div>

<script>
const hamburger = document.getElementById('hamburger');
const mobileMenu = document.getElementById('mobileMenu');

hamburger.addEventListener('click', () => {
  hamburger.classList.toggle('open');
  mobileMenu.classList.toggle('open');
});

function closeMobile() {
  hamburger.classList.remove('open');
  mobileMenu.classList.remove('open');
}

function toggleUpdate(header) {
  const card = header.closest('.update-card');
  card.classList.toggle('open');
}

function openLightbox(src) {
  document.getElementById('lightboxImg').src = src;
  document.getElementById('lightbox').classList.add('active');
}

function closeLightbox() {
  document.getElementById('lightbox').classList.remove('active');
}

const observer = new IntersectionObserver((entries) => {
  entries.forEach((e, i) => {
    if (e.isIntersecting) {
      setTimeout(() => e.target.classList.add('visible'), i * 60);
    }
  });
}, { threshold: 0.1 });

document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));
</script>

</body>
</html>
